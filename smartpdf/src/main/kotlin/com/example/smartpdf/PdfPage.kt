package com.example.smartpdf

/**
 * Lightweight model that represents a single page in a PDF document.
 *
 * @param index Zero-based page index within the document.
 */
data class PdfPage(val index: Int)
